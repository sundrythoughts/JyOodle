sum : int
bl : boolean

start() is
	i : int
begin
	i := 1
	loop while i <= 100
		sum := sum + addFunc(i, i + 1)
		i := i + 2
	end loop
	writeint(sum)
end start

addFunc(a : int ; b : int) : int is
begin
	addFunc := a + b
end addFunc